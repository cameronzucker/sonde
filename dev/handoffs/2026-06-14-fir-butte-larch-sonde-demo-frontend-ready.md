# Handoff — Sonde interactive demo: engine done, frontend planned

- **Agent:** fir-butte-larch
- **Date:** 2026-06-14
- **Epic:** sonde-669 — Sonde interactive adaptive-modem demo (live WASM)
- **Branch / worktree:** `sonde-669/interactive-demo` at `worktrees/sonde-interactive-demo` → **open PR #4** (https://github.com/cameronzucker/sonde/pull/4)

## TL;DR
The demo's **engine is complete, reviewed, CI-green, and pushed to PR #4.** The **frontend
is fully specced + planned but not built.** Next session: execute the frontend plan via
subagent-driven-development. There is nothing to "see" in a browser yet — the frontend build
is what produces the openable page.

## Branch / working-tree state
- After the final push, branch `sonde-669/interactive-demo` is in sync with origin; working
  tree clean. (At handoff write-time: 1 unpushed commit `812e2ed` + this handoff, pushed as
  the last step.)
- PR #4 contains: the design spec, the engine (`sonde-demo-builder` + `sonde-wasm`), the
  engine TX/RX additions, the frontend spec, and all plans.
- **Do NOT touch `main`** (ADR 0002: direct commits/merges to main are blocked; PR-only).

## Worktree stateful / untracked content
- `worktrees/sonde-interactive-demo` is the only worktree for this work.
- `.superpowers/` (brainstorm visual-companion mockups) — locally excluded via
  `.git/info/exclude`; safe to ignore/delete.
- `demo/site/pkg/` and `demo/site/assets/source.jpg` are **gitignored** (`demo/.gitignore`)
  and will be GENERATED by `demo/build-assets.sh` — not present until the frontend build runs.
  `demo/site/assets/payload.bin` + `payload.offsets.json` will be committed once generated.

## bd status (epic sonde-669)
- `sonde-669.1` Engine (builder + sonde-wasm) — **complete**, lives in PR #4 (not separately closed; closes when PR #4 merges).
- `sonde-669.3` Engine TX/RX byte additions — **closed** ✅.
- `sonde-669.4` **Frontend build — OPEN, P2 — THIS IS NEXT.**
- `sonde-669.2` Map-linked "propagation game" mode — open, **P3, deferred** follow-on (browser HF-prediction; study Tuxlink *Find a Station* at `/home/administrator/Code/tuxlink`, PR588).

## Completed this session
- **Specs:** `docs/superpowers/specs/2026-06-14-sonde-interactive-adaptive-demo-design.md` (parent), `…-sonde-demo-frontend-design.md` (frontend).
- **Engine:** `crates/sonde-demo-builder` (image → SITREP `payload.bin` + offsets) and `crates/sonde-wasm` (encode via floor-wblo → hf-channel-sim → decode → BER/SNR/per-symbol/STFT; `list_modes`/`recommend_mode`/`run_link` JSON API; compiles to `wasm32-unknown-unknown`). `LinkResult` exposes `recovered_bytes` + per-symbol `rx_bytes`.
- **Plans:** engine, engine-additions, and **frontend** (`docs/superpowers/plans/2026-06-14-sonde-demo-frontend.md`, 11 tasks).
- Full Rust gate green: `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all --check`.

## In progress / pending
- Frontend: **not started** (plan ready).
- No blocking decisions. Settled: style **B** (modern dark dashboard, Tuxlink-vein); **standalone 3D waterfall** (no 2D scope); **TX|RX console** + real corrupted-image reveal; demo image = **NOAA ERI public-domain** (fetch recipe in plan Task 1); build it with the **frontend-design skill**; **proportion discipline** (no full-bleed skinny elements).

## CRITICAL first actions for the next session
1. Pick a moniker (`python3 .claude/scripts/get_agent_moniker.py`); use it in commit trailers.
2. Work in `worktrees/sonde-interactive-demo` on branch `sonde-669/interactive-demo` (NOT main).
3. `bd update sonde-669.4 --claim`.
4. Do **NOT** re-brainstorm — spec is approved. Execute the frontend plan task-by-task with
   `superpowers:subagent-driven-development`; Task 5 uses the `frontend-design` skill.
5. Run `./demo/build-assets.sh` early (needs `cargo`, `wasm-bindgen-cli`, `jq`); serve with
   `cd demo/site && python3 -m http.server 8080` (no `file://` — fetch/ESM need an origin).
6. Keep the Rust gate green; honor commit trailers + no-destructive-git.

## Honest caveats baked into the demo (keep them visible in the UI)
Only `floor-wblo` decodes cleanly, and only on the AWGN-only `"none"` condition; Watterson
multipath degrades/fails (no equalizer) — framed as *why adaptation matters*. Full-speed QAM
modes show as "pending" until the parallel PHY work lands. Channel is simulated; not on-air.
